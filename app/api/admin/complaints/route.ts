import { notImplemented } from "@/server/http/api-response";

/** GET /api/admin/complaints — implementation pending */
export async function GET() {
  return notImplemented("GET /api/admin/complaints");
}
