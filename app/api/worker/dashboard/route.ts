import { notImplemented } from "@/server/http/api-response";

/** GET /api/worker/dashboard — implementation pending */
export async function GET() {
  return notImplemented("GET /api/worker/dashboard");
}
