import { notImplemented } from "@/server/http/api-response";

/** GET /api/admin/dashboard — implementation pending */
export async function GET() {
  return notImplemented("GET /api/admin/dashboard");
}
