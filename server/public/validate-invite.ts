import "server-only";

/** Validates an invite code. Implemented in Sprint 4. */
export async function validateInviteCode(_inviteCode: string): Promise<boolean> {
  throw new Error("validateInviteCode is not implemented yet");
}
