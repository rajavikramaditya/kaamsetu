import { notImplemented } from "@/server/http/api-response";

/** GET /api/admin/payments — implementation pending */
export async function GET() {
  return notImplemented("GET /api/admin/payments");
}
