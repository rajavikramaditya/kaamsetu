import { notImplemented } from "@/server/http/api-response";

/** PATCH /api/worker/jobs/{jobId}/status — implementation pending */
export async function GET() {
  return notImplemented("PATCH /api/worker/jobs/{jobId}/status");
}

export async function PATCH() {
  return notImplemented("PATCH /api/worker/jobs/{jobId}/status");
}
