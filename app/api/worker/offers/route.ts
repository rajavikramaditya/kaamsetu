import { notImplemented } from "@/server/http/api-response";

/** GET /api/worker/offers — implementation pending */
export async function GET() {
  return notImplemented("GET /api/worker/offers");
}
