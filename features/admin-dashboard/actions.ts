"use server";

/** Server actions for Admin dashboard. Implemented in Sprint 2. */
export async function admin_dashboardAction(): Promise<{ ok: false; message: string }> {
  return {
    ok: false,
    message: "Admin dashboard is not implemented yet (Sprint 2).",
  };
}
