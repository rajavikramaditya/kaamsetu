import { notImplemented } from "@/server/http/api-response";

/** GET /api/admin/jobs/{jobId} — implementation pending */
export async function GET() {
  return notImplemented("GET /api/admin/jobs/{jobId}");
}
