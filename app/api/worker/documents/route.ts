import { notImplemented } from "@/server/http/api-response";

/** POST /api/worker/documents — implementation pending */
export async function GET() {
  return notImplemented("POST /api/worker/documents");
}

export async function POST() {
  return notImplemented("POST /api/worker/documents");
}
